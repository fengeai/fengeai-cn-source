server {
    listen 3020;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location = /assistant {
        return 301 /assistant/;
    }

    location /assistant/ {
        try_files $uri $uri/ /assistant/index.html;
    }
}
